export enum DiscordBackendEndpoints {
  Oauth = '/oauth',
  Me = '/me',
  Guilds = '/guilds',
  GuildConfig = '/guild-config',
  Logout = '/logout',
  Messages = '/messages',
  MessageConfig = '/message-config',
  FullGuildConfig = '/full-guild-config',
}
