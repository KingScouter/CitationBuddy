import { GuildIconPipe } from './guild-icon.pipe';

describe('GuildIconPipe', () => {
  it('create an instance', () => {
    const pipe = new GuildIconPipe();
    expect(pipe).toBeTruthy();
  });
});
