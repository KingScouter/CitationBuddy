export enum DiscordBackendEndpoints {
  Oauth = '/oauth',
  Me = '/me',
  Guilds = '/guilds',
  Guild = '/guild',
  ServerConfig = '/server-config',
  Logout = '/logout',
  Messages = '/messages',
  MessageConfig = '/message-config',
}
